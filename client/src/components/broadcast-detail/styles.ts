import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  input: {
    marginBottom: '30px',
  },
  list: {
    listStyleType: 'none',
    paddingLeft: 0,
    margin: 0,
  },
  total: {
    marginTop: '30px',
  },
});
